{"_id":"asyncBuilder","_rev":"221-f52c0974f69525279105aa181063cdda","name":"asyncBuilder","description":"handle async dependency loading","dist-tags":{"latest":"1.1.9"},"versions":{"0.1.0":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.0","homepage":"https://github.com/azulus/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/azulus/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.0","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.0-3","_nodeVersion":"v0.6.10","_defaultsLoaded":true,"dist":{"shasum":"38533944edf024f7a0aa10b07df03481dd2e3fc5","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.0.tgz","integrity":"sha512-nycBYHkAoKEv9MEuun+kdqBrcapALML3PxffN5WY7niRUtziXEZQgVt4kIWEM6phwMdKMT0rN8EDdpOos4b15g==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQCnOVcOsOwjaNei/otYxI8cOvZiXmlcYs/P8MFkOhTSXAIgNieKiS8H/P8LEhEOEncdzfVhKrx3QsuT1HK4uBFFIpY="}]},"readme":"","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.1":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.1","homepage":"https://github.com/azulus/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/azulus/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.1","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.0-3","_nodeVersion":"v0.6.10","_defaultsLoaded":true,"dist":{"shasum":"177a52d5a51534ca3759a5743ae1987f32867b5d","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.1.tgz","integrity":"sha512-HBA+aeMANIZrSsXE5xuaz8SkcMj5FMig/5+XGswMiJ4znZQfFEqhIyoAv3CwRFYsvUiGZMYej5ZkoCQ3gFtvzA==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQC9MTN92uiJVr1us8GE5PteUNiISG1jmPIqAUm1AblcWQIgaWkcFtqfE2b26iR//ABDsQ9jbX5NkBetT97wanyUwhE="}]},"readme":"","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.2":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.2","homepage":"https://github.com/azulus/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/azulus/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.2","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.0-3","_nodeVersion":"v0.6.10","_defaultsLoaded":true,"dist":{"shasum":"905c1dde5209083c82ae1d8da52d4725f41af696","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.2.tgz","integrity":"sha512-B1ZrST/ry65JMweAeIuyWlvSv8pLSkrS1K/eXK0+2v/BXmSjEfUywrGu/ZhhJQFjQdU4CuYu7Ou21WG/yypDLQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIG9vGgjqc78tnGhI9HvUTSDTvd2aMv0g3fR65cgKEt8yAiEAlGk84W7vt9lsfba893YqHtPS7rTS6Lg93wUqcrf2yuI="}]},"readme":"","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.3":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.3","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.3","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.0-3","_nodeVersion":"v0.6.10","_defaultsLoaded":true,"dist":{"shasum":"bbda0f0ccd89ddca5554056db9d8f6210c488008","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.3.tgz","integrity":"sha512-NeZrJmeoSz2fJgTJWFCN4C40/1AUBQpfYtASb5SgpbzV646i/4XCNBeTkD21sBXuhZxV6/GSXHvL+sliuOJzcA==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQChmN0qIgeYuUOpY+Si7ty+jQ06kascTqMDvG/ISxUZDQIgOur27ZxGAiDPHhyw+OFsxsf/aKef25MKgKKDordJs8k="}]},"readme":"","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.4":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.4","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.4","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.0-3","_nodeVersion":"v0.6.10","_defaultsLoaded":true,"dist":{"shasum":"5921f9d3520e746e7e7df1684d7607a6e610365f","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.4.tgz","integrity":"sha512-M1UXV6UH04b3Ji4+1MznAIjlZWi6htIhdvVAJ1JAPXDANsa5b+sB2F0d2TdSnH4PvzE+sehspuntsIeeLjEeQQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEYCIQDZ6TViGg0dldPSqNfYI5O9xpzWads7w1j/gtjDAsWwlAIhAJGSL5kDtE4yBoNF1Eif0Z8Fn+yVW2625JqB8QIsqabB"}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.5":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.5","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.5","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.0-3","_nodeVersion":"v0.6.10","_defaultsLoaded":true,"dist":{"shasum":"231a77970a1cf62a892b5f1f67e1522ae489d1a0","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.5.tgz","integrity":"sha512-WSSP1oaxVtdbNGbfn7igjDMId77f69ebfPjEpTGxuzG58XThbBaiKAa+9ctWNCq1UE9C+CbmfMybKtxzFjm5iA==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQDxEbPvB07+MjCOVQqmdqO2S3skz5gGBO4jwirA8Ij5hwIgDgCzvvHbhQMti8o6fSMuF4o+Shimss4CohwS3fjm508="}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.6":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.6","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.6","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.0-3","_nodeVersion":"v0.6.10","_defaultsLoaded":true,"dist":{"shasum":"5f4960da2633c3cc5e68423b7e7397e076688773","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.6.tgz","integrity":"sha512-30XX8SYVJNhHMZLUYpnJ2XxEcvllI1n9y5NQCEv/fCersYpwBikgeGZshoTm9tBFj2ooKOhZ0dbZN9KRgg3WTA==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCIFrcZqIDZ6sCA3jBuDCHOO+n6wOy2uZYs/2vRbuqCuQnAiA/EnuyYE3V4KizPLA35AHlvjtg1HLeKrpUztD3UiEFqQ=="}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.7":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.7","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.7","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.0-3","_nodeVersion":"v0.6.10","_defaultsLoaded":true,"dist":{"shasum":"54f89be6ddd542f78f25edf60fd38b34b81865ed","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.7.tgz","integrity":"sha512-nezwFnobtnyoIa9SmioO+dkn/GGDl9KvTjHJWtT6h9lVUo6C1mSt09ol8QPI4EGF4B3psZXk/saqa6bqYSN3iQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCIF+8XO2E5fwls9BJ50VilAeBanAUiAN8ReqQHxxC0a7iAiA8aiqoc80udpjOvcDscxiw3qhBBzJuPfKLGr0Ix/cCiQ=="}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.8":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.8","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.8","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.0-3","_nodeVersion":"v0.6.10","_defaultsLoaded":true,"dist":{"shasum":"6a4db4158c2c0e20337b65981cb195f14e6460de","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.8.tgz","integrity":"sha512-krbiFqqW/cy3IfYwV6NvdmWpoynBkSCCr5i3+WkMkPEYRjdQMJtWwCBEFlBjmWSHwgjPfj/w+VEOsg/TgSC0MQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIG9/J+NIlNtbbM8z06rfzn5xX0tNi4S02sYahugthOqWAiEAo9uwg0YWCIa1p2rPbo5rgGuVpAV3u/zrpgGQqzK87MM="}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.9":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.9","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.9","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.0-3","_nodeVersion":"v0.6.10","_defaultsLoaded":true,"dist":{"shasum":"9d7f3e402a95277aaa1c45e2ad43251216095339","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.9.tgz","integrity":"sha512-7o4clV+6QIMVeBpPfDzatT1GwtHn4jkS6keKtVMCqQyI0yJAwJPWYEErEZmufbMZO0nR57O2FSPxq2eHdyJnoQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIDOzo8hIXOyMmvT/s0uspLi/23q/KpjyLfZPlNWsewUrAiEA/bk7DatX14GnYkAIqV9qfjPBrhpuT0aDVip5xq5tvGg="}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.10":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.10","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.10","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.0-3","_nodeVersion":"v0.6.10","_defaultsLoaded":true,"dist":{"shasum":"6308f2da4edcce32a48385bbea24d3a17ba716c7","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.10.tgz","integrity":"sha512-NN/c3JzCWoQKZQ+Rg6XpYQBpyOu4/C+ZroGukpAGfnhJHhAjkQjUXOQHNHD7/AO4n6I+/f433zywIPN/rh5Y8w==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQDLg225Iak71PIEp0lpfTytKWOx+v7afucQdRUlXVYt9AIgU0oaPAybq7aTBXqfdJCnJrlwouKA9Z5kDWeyO4IULGM="}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.11":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.11","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.11","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.16","_nodeVersion":"v0.6.15","_defaultsLoaded":true,"dist":{"shasum":"3ef1a42f13c1c281d37752b44b7b45dd3eb93add","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.11.tgz","integrity":"sha512-YSovFbuByeMsE0IzCDHeKRpgKb1ZwM0hV4qucnhsSmj++ip0rAlk5SfMWGCNbwU/Z7mOomkJr6gn9/zfH4Uunw==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCIBfhl3mFYNeWaPLzDeW8Ke8oFScwOw0Z5lZNNRKpTEt+AiB2p3BgUq7gwciQbyb3XK+VBTi21YfLkmukam1QkEJjKg=="}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.12":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.12","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.12","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.16","_nodeVersion":"v0.6.15","_defaultsLoaded":true,"dist":{"shasum":"5f8badcbd2a6ec6c1cf992ebb878a34eab7964d6","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.12.tgz","integrity":"sha512-RBPam5mxQrKwqjHniLCk6dC+1hf0a3j5jpZ9nOuaH01Jg97pjIrCjdex2wIFGq2kxJWV/KbhhH3lLM1vZuqm6Q==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIAbtIfQ6x7x4puNmAxtOsN2JPBRFf/Ji45QSyZeBn7e0AiEA/rdFJS6aSXUvmdhjaaxjtmDRITPSHQac26uVkzhptvk="}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.13":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.13","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.13","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.16","_nodeVersion":"v0.6.15","_defaultsLoaded":true,"dist":{"shasum":"87152f828dc6fbfc5e302da35c0977d2ed44f7e0","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.13.tgz","integrity":"sha512-JaQ5golT+bj1lTwcO7cp8myQnQItyQz+cL7fF9sx4hZ5wZ4L3SqDjyi6hrB1dj9HfwnAV5dupOzxHA/bh2FQIg==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCIEoirPSGrU7SOUygXJQXGIvn0qbiSKFb3Pt6428HB1GsAiAapC1zPA+u58ZXSw/pLttyqQqQcchEXE/fUtJ+hlchzw=="}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.14":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.14","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.14","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.16","_nodeVersion":"v0.6.15","_defaultsLoaded":true,"dist":{"shasum":"b4f58f87c413c6f5f84335ef9844b0fe7619cdfa","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.14.tgz","integrity":"sha512-td0SkrDRVp6X1iuoatdICSW6YIGR+B3Rom5/JhkAGMQm0riK55BKqDy2TKPNfKek/lC4bv2d0L/9EtIA7uPzYA==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCIA0w92tP2Zx+Oe9QMSbwCLJi3GPEicd9lMdP2zQH4A1TAiByLZFvBa2fyxpS3whvgGcI1f59AHsJDfIU2BpGmPV0KA=="}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.15":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.15","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.15","dependencies":{},"devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.16","_nodeVersion":"v0.6.15","_defaultsLoaded":true,"dist":{"shasum":"25169dd4252994ab9642279fbe7d65b8db757c30","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.15.tgz","integrity":"sha512-pUImTAZ6lRm2nTiUidq5JOIF/v7r8opHe+yai9HDR0qKCFkxa2k9bDyAfkQ11B6vA2+iLv0zCJrDVrimWgoVXw==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCIERzqT7rxHK5QQZEvU/50q/fT8QFnQyDGsoMXmP1odw3AiB13DwWrBZ5noV8CPakCU3Ay5wXSzhgSqYsPnVNF84jrA=="}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.16":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.16","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"dependencies":{"q":"0.8.5"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.16","devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.16","_nodeVersion":"v0.6.15","_defaultsLoaded":true,"dist":{"shasum":"1dbc4f881485a84c52b73c6e58463e7474272d62","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.16.tgz","integrity":"sha512-sYnciaYbxc63Jwj3fCgb5tNPGoE5YL8QC7RoQANoNsfOu62bCbk5pqcxk+/nAKzPDvmNLfjn3j+NL93gDLobUQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCIB9Amdv3iKvCmlv8u6QV8MUNIz4ahF2Oa9jF9J/sx3H0AiBmMfHqd/wS6Uj/C1hdVcsqYIkjEWmhJmIK7Hs55gdSkA=="}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.18":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.18","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"dependencies":{"q":"0.8.5"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.18","devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.16","_nodeVersion":"v0.6.15","_defaultsLoaded":true,"dist":{"shasum":"998bc365781a761735298d7c7998f12a03eff1b7","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.18.tgz","integrity":"sha512-Kvui073MXoRhPT19bv6WKKrVMTmwg7aiyOw9TqNBH2cKsTxBiuomUsyz1JlodIYNrZ76bQMkopVPHgGQ7/cgsA==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQCNQ04869lh+E7AA5zQadd9m9ybv0HJMJlCEd0Z/oRJigIgZHiXmQY5ph8YcaHoOS+o1EDJZvlePV2Cnm0zFvZXbUM="}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.19":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.19","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"dependencies":{"q":"0.8.5"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.19","devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.16","_nodeVersion":"v0.6.15","_defaultsLoaded":true,"dist":{"shasum":"8b988751daf183c69edfc721b63b4e3a0e988003","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.19.tgz","integrity":"sha512-IXpHVAOjhUkLQykmzco6BEjyyPC47Q3+ejWNFabmS0Sv2EbkoFnK/3/PZiZtP6mdpPJHuUf6yujnbx5FCks3tg==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEYCIQCsDYcavBXkZuNI9BJbkY9Qvo0IWe84a1Vf9WbHdEh97gIhAJJV6BLeI+bMonTmjO6iCdKP6OX7WS7nvdLFFgiUo6UC"}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.20":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.20","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"dependencies":{"q":"0.8.5"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.1.20","devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.16","_nodeVersion":"v0.6.15","_defaultsLoaded":true,"dist":{"shasum":"54259ec59177d1457def6b5b065cd5e97630c218","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.20.tgz","integrity":"sha512-0TQs1bwwHXsidELUxbXzs12UEdt/wKZGwjjf95ribJfwgUuWlsFm0fdokvBUKDXdPi5GWc3JmdV3gu9g6pCAcg==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCIFfaNUlAWkhqHdNu60pUn3jaX+GYCiFQR/07O2Iu4ABJAiB62OcjYn9XRMB/VHy5eiuS9xSmgxDeMrim1osLC7walw=="}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.21":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.21","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.1.21","dist":{"shasum":"a4299a0831b4c8f364b44e9aeb0fc78f8730b876","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.21.tgz","integrity":"sha512-6aA1aHxld9EV8A1u6GTw7FKbWm6X5QLlC58K5DvLZZwkn2sx/Moxu5v0BHkdiozKpOaK+ljVUGr1MGUCZgT74w==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQCH4a10AXshi4G6dIx2YNPVCawOZ6rtwh4WyuWLCCEIjwIgZaZPs/A8Z71st2pz3BY+G5UyUTKch0rEdxVRyVROd8M="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.22":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.22","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.1.22","dist":{"shasum":"b60ae4f135f807f3efdcb7fe81cf28260339d978","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.22.tgz","integrity":"sha512-hTHk5mzeEL/Iw0lFDqRh+/h1LkqirH0HthPGAoCOX5LVc0d3AuhH3X/6pGXJrIit1rK9K4h65VG287/SHajGmQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEYCIQCH0ZyIVaY8so9uk7T/2uhf3w/9BDxw8EsDdtF6uemU5gIhAJfk0KzFXPzObR681Lc1pH92p5YSUItDayg/QyAgDCg0"}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.23":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.23","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.1.23","dist":{"shasum":"9843d7162e590d32ac2df227d0ad179b358c4438","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.23.tgz","integrity":"sha512-1iPsSIGd+d9pRcWmQmJjInnCYmww+Z8mdHtQp+fTN8AbqdqxhV6r/gbNyulpO6vhHlJFCIEDthtqnabAAonYGg==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIEbOJ9gDmhiCLl30RkEJGGHIxVD7BjDlx1iQija54TrlAiEA06YkIOBnldNLMzjSEUjE3B7aShiw2l08ShXs7es9PFo="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.24":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.24","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.1.24","dist":{"shasum":"a93a14513a6d1a376a8096acfd223b6b4ef9775f","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.24.tgz","integrity":"sha512-J65suEhR9xGIOPc/fICbqluXOqNrTCKqABrDzHCQGKLuIfQOJ9/50ga7d3VDKhhbRFfWUSf2I3CMeGKv5wrdQw==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEYCIQDB5em5/q+ZSrZStmA2BTm5GGyHBIBey/47o2rpqHACLwIhAJYCiDt2EltuwPBS0ND/z6SESXTGK/DdU91Z1pbpya1W"}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.25":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.25","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.1.25","dist":{"shasum":"2506e9275f21875de3888d1e6e03c827878c977b","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.25.tgz","integrity":"sha512-0rX3OW9T1Qb7fp7aU78WjiEOOG4NOdKIMg8K6OKCJQgxwnSjmDlGGOF1a+IjjjMu2YyFXhYU4XEz66OXdhTkCQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQDGJ+0m+jiGe3E7yrceUNKx1dNG1qCiA9cXAOHnKnQqdwIgUncvhRc1S1O6TX9qb2nt+sTf1yu4u6Q5gt7s/db2dgM="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.26":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.26","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.1.26","dist":{"shasum":"9fcae5e8f48c8cbf6157e48d59206fc612fb69ab","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.26.tgz","integrity":"sha512-5P2rA4na2WNtulHzS2JHPa+AOWATN2ipTubs/rOjvJ+SOZSwXahwND3xo452NDdX/5sp50+YM3zJgj+zsa3yug==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCIB0U+mt7wPONSu3faqDoXFDeeinp9LgnQCt5aoa+qAA1AiAo9Y2Nd6qdJvkpjWU5C9gtZMnMv6AD3CkHkWQC6y+42A=="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.1.27":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.1.27","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.1.27","dist":{"shasum":"1f93477a5b92083a6fb3372d4b32a6f405a54da8","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.1.27.tgz","integrity":"sha512-0WM4hiFcJZi2l8VMOXAkSElCO7qTbZWX9W+9O5idZQHoz5+VrNaAAWzWH1ZRhHFmE+SEEzEJPGJdG6YhcWspPg==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIGoDQ7ZrHXJBKQinU2P3jlfJzFZB6hwZ6i98T2hZWMmLAiEAmi62E/DBWCllU+VTTaUZair86D1LxscWqLm5WKigW+M="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.0":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.0","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./lib/asyncBuilder2.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.0","dist":{"shasum":"46382df8827ae95cd751aef30e3f2b36c9b425ee","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.0.tgz","integrity":"sha512-/+2wgv05PKLzk4DDSMk8x+A7im0oVj/ZdBMu5Vj2EzlUXehEG9g5juoAq3tJot0cvRTqw3hmrO2EiqqyoMGL/Q==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIEZ416xYhNo+lml+YwHLKvJ+y4KBPubx6DgFUPh/zolyAiEAyDrURosFJuBN5t6lImPSgKB2BdQrUMRf7BRUlJuc9Lk="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.1":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.1","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"./lib/asyncBuilder2.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.1","dist":{"shasum":"1492d64f526cf2e07d6f683feb3eabbace6ba8ab","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.1.tgz","integrity":"sha512-i2mQaMRgQCEK11lwqrEINvzo3tdY5s0X16nJ8fWe0s9jJtl0KfoKPaX4Y99ODaElS+VnE2NFICu0xAE55e/45Q==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCIGfMuIM6KLPXka+QIEkLatZK3DTKaudMHBjtgIsqLEY/AiB+rNG/z3ZIpRstPrlrwcIG1vcsTMfrYwyM5gDfDs6xVA=="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.2":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.2","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib2/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.2","dist":{"shasum":"3413752d9a377edf35095fea1b43d34c6dcfcd5f","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.2.tgz","integrity":"sha512-ARL8FfRyTe21dIheA14KdadaRZhbj9lQtj6KCaq01NonaAHT2eTWue1uhxOY7fdFRGfJp4Cp90clA50rhXFJsg==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIEzwTH4KbPFGzBKnkruXDxcGJ46EuwZyHwA00KDVZ3SXAiEA/OyALVuf2t5Ua0hrfKM/aC702xNFMeJoNKfka5S9udk="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.3":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.3","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib2/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.3","dist":{"shasum":"3894af8d8d0698ab41fb490b8d6b68a4c9877e1a","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.3.tgz","integrity":"sha512-c+jT/4VmiGtSK6vRWWAfbGTzi0ZEcxvD5rSClTCugyGf2fbq8U3LPPkAx6a/025cmtimOYMGE9SX9I4wypcuQA==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEYCIQDiCr1mDcpDCHBiF8tsQiSqzzt+8LzaH/bzAwUb4+Z9TAIhAOzWA88F2v07GXWTFS+w9urqubOaDH2/EiJbpb9YkvZa"}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.4":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.4","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib2/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.4","dist":{"shasum":"dfe1d59d9e08989ff23791e485384b48144b0bcf","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.4.tgz","integrity":"sha512-fmG2yEvUyQDuemDASM0aWpEa6oOzroEtUcTZ6P+Sy7jIqAA7xDdnqku+3kX3l7FPcsQTMPRlDmj4SsjqOzXWag==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCICrE4FUFJ155tNa6RjFxsQMlDLLeNT4swVLDOHHtDHVwAiEAp8Z39KaOe3iJb7emI1iPAS36nqD65x6pmYgsiEdSToU="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.5":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.5","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib2/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.5","dist":{"shasum":"10de045a940e1ec6f1c919e060e4b56d47ac4b4b","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.5.tgz","integrity":"sha512-Gf6PrmwasACqtqTziU78Fo7Yl26gmtq0A6dpFpFaSsS0+kbxFXIf1v+lsA3/qUUwARmISGU9S3Aa5hzRoO2SIQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQD7qZsGMC2K8fQ0q/lQro3sVi2LeE/vooPVktKyANjKcAIgTd98kTuQml8DMNVrpp4EVhfr+MhUCq0+kHSJcpxB4J8="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.6":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.6","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib2/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.6","dist":{"shasum":"4bcce5979eee118aba2d143b78f3a0e185bc7728","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.6.tgz","integrity":"sha512-xnfbAiNCZVL4B2dxhnv8T1IxcdrCVc5+plezlDmWm3K4Q3571/MYPFY2F0xMGZf/qLbax5iLrsixyBSfBqxI6w==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQCUDKbOo21IQyEtGU3nLDEv1DNLPfLj8Bs1b9JozYay6QIgQzZyLYvfzIUH8sxFybWGK2AUaaCPdcEnAEM4boukOSo="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.7":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.7","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib2/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.7","dist":{"shasum":"e9ede3083550127843642637319d3632827ed1c3","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.7.tgz","integrity":"sha512-ldReSo1tupAo0/a0PwRHVvaIZhKDW0+9WgmPvn5dSuodVe+Z2PMvhqOMvEGbBPBkMM/ZamF6ZNfnpVHDynutYg==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEYCIQDRY8yy6CFhQs2jCnu1U/nivMdEe4g+gS2DcvfmCB0RRwIhAPwuHskgCMW/yWgVbwTRfz0bEuVhk6H9scGghplGqyLM"}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.8":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.8","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib2/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.8","dist":{"shasum":"94271d3cb938f3c61dead70da8fa3ff0bdb216bb","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.8.tgz","integrity":"sha512-lbDVMuWv4W6qCjduCjy8tmYEI66xZf2+t//xjVmBRsmJd3VhSp9cbxs8FO6skvOiYZc0VMpXMedopnb//Z96SQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEYCIQDeqi3zIJbtRF7zupbbrkMt2Zk0GpcJ0y2zwSPE24glfwIhALnmq8rBv/rrkOBUL/FqHdojqgE67HAeGkLUKAxz1w5t"}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.9":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.9","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib2/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.9","dist":{"shasum":"afb865f53b3a6343b0679d59bca51451d8f142ee","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.9.tgz","integrity":"sha512-jWIKax+EuL9pKoPdseLphLolUZV0lvm1vmYnWfI7YLDUySqYvlUqgUnEmMwXqZ4oeA6/cDFNrrngO8zorLTclQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIC28ZYehVBhbmHf73fKk4wcuyaYdfZimoJFzCej0W8D7AiEA0HSH48ZQ2Zpy2Bu/xnSS3RH2KtlAlOQLO0NXV3MLgjk="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.10":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.10","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib2/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.10","dist":{"shasum":"3b801400a73bb733f7ad8185539272be6a8f6c5e","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.10.tgz","integrity":"sha512-7SPME4EUOTy7hp0pKwU1eAGbL11wNmFNWmd+zxnT7hBJOIw+w9Fj7kZFMlddMT6YzpE0xbkPyKy3sntQhJmUeg==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCIG+zqlmLEwbzQJNmRTsz2IWwH5wb+KWgoM1JbmI4hRqgAiAWJd3kw/pTtJvgDlmcKWNviiP/Jw2vdhRGcCw+JBa15A=="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.11":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.11","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib2/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.11","dist":{"shasum":"975c100889cebcc15921115492349460f940c148","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.11.tgz","integrity":"sha512-CBefL/z55oOOeTP2PT7NQL9ZzmSqQSoYgAJEzlHImRUmWVcxg8NQOZ5ZHftzyHP+3fUTMeE8Z1wTRv6ijaR5yQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQC5fkuQ5RZeItU7M6OfcPGMYrLeYI4tWIAIV7FVgT9OqgIgX3r2Ki0pebryWmV16SnvGe63Z95jPlY5737TJoCAsKM="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.12":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.12","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib2/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.12","dist":{"shasum":"77525b65f5930c3a3d31f1ff4ee7e8250e8ccdbd","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.12.tgz","integrity":"sha512-prYIrcL+4ebsDJTVK1etJpbPGLVY9ou/JN7l9TL6me0xPV1IaiXkxHh8ugFVnY4WRVtnhCOhY04upo4Ybbytag==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCID1cN+S+SZZ1kkcvLE4m357Z05OCaGNcAFSHLYe/ZrLXAiAxlRj8qs/AGC9s8Mdrvkt20bG83QeQLWtJKQUMi9SNKA=="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.13":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.13","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.13","dist":{"shasum":"ac6b5a2572d631b2fc6808f5552527651ae494b3","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.13.tgz","integrity":"sha512-0iXtjM0TpPaw4JaDYA/x1Lcld5zeSFcVgLFOMNTphQqz2wB6MvPpSChUcKCbDad81sssD46euzh/9EEnnC23Ew==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIFG71yEcrZPQGBLBvbAeEX0BTPBPjOhkz/dDyO8P8nRpAiEArmK8ZYcPJbU42NXEwnt93ulLGjKu6jAhtv7UL3ugmS4="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.14":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.14","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.14","dist":{"shasum":"136e5aaf01543f7744c0693be86faa9c23afc24e","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.14.tgz","integrity":"sha512-vUPDN6TtcjGmCTQUz1skXwb7UmlIB7J7OSRmvg04Yy/qEsFNF5lJsJJbdyGn1cjZECpQlsPB077B+qISZ5FB9Q==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQDcvNJanylXIAhyPk4fUIi3bz7KL/hg/U0JdunV4l3jOwIgK/1EzKv5ZNwb247OPP9ISN11bU89GXHZXcLxdI291SQ="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.15":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.15","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.15","dist":{"shasum":"8d89d3117ddef5b57786dc11b54dc8fc098f0fd6","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.15.tgz","integrity":"sha512-BShSqyolagzivyMcFA1StOJ+BM3NOXV9U3ODkABSJETPKgwZJ2jOFEHf3tYTr+CCMUIsc3RmYkp9NEvZNEDadQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCIB2u86YIcBuEVtmtZZ3Gngy79urMzo/6DK4MMTyLT8M0AiB858xBWDuimF7FS2byFP+AfrGkc+ggBQKfOuGkVTY9kg=="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.16":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.16","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"git://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","q":"0.8.5"},"_npmUser":{"name":"azulus","email":"npm@azulus.com"},"_id":"asyncBuilder@0.2.16","devDependencies":{},"optionalDependencies":{},"engines":{"node":"*"},"_engineSupported":true,"_npmVersion":"1.1.24","_nodeVersion":"v0.6.19","_defaultsLoaded":true,"dist":{"shasum":"2a909f81b0658b5a11395556f625060586b3c0b9","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.16.tgz","integrity":"sha512-VmAOQCR7SYOJYlqAUBV92noszEJcDL+AdhkG55yQMW1e2bi/MpJBk+wngGoHKH7c3SnPF40xLe1ibAXYIwa34g==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEYCIQCG7zTMUHBBTpUYrYUBAsJ+8ilbqRuKYSNBDMEW0L+lnwIhAJSRMUrkGEQ2lRZSHMk5j9bJk2uHB9ZmktgoGSX4C76l"}]},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.18":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.18","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","kew":"0.0.2"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.18","dist":{"shasum":"5a3cc6a8d7c28af408e40b60ad6c63a509f49d24","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.18.tgz","integrity":"sha512-QwCriV7tFFbLYKJsr7sNwn+cF+XINpWIoZwKqHPvRdY7H0VYuDOzg0zcLtk4lGGRN9eFb6ilye9Sk2KdvqiL0Q==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCIHQ1dBWkbw7/31cXICYDcNKA2BvIVvzb/ZVhZmLHyY5PAiBDpIk0VIbzRSG5eqeoojugqHNr6NC6y5ZnRAMmqU+p/A=="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.19":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.19","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","kew":"0.0.3"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.19","dist":{"shasum":"182981cac442ae5af4e958cfddb73ffdf6d58f2f","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.19.tgz","integrity":"sha512-L7cBcEZ7dNa5zGYtjCesTw7QSMFmnv5QyKK+QkcXr7Xt7Hg8iHRm40GhVIu9qdk0MnVtNTnxRgrJrsX4XHzgJw==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIBPfmsSwprhSopqqbqof8y4DRa1oY6enwsAUzMmwylHeAiEArIJ+QRTmJKjOAo/nm4cKBjfoAhKnaJM26X7UDfnwdpU="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.20":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.20","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","kew":"0.0.4"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.20","dist":{"shasum":"0f85fbac7d93807477521cd8f918a3d4932e9acc","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.20.tgz","integrity":"sha512-D914p11+MufIVqiittlf5F8N8cOU+yc2okWqnyOsXaocK7JHYcDyP2lWuxw88H6GFWekb4NYyB5PqFwaBIJ4YA==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQCXePBPHxaj5AkolEyoz54ylLUEvW0xKnXAcA+04KZO1gIgT27fAbA7pjpAncO5/1YraWyZ2VppFBjNIyjEvRkoZl8="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"0.2.21":{"name":"asyncBuilder","description":"handle async dependency loading","version":"0.2.21","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"0.3.1","kew":"0.0.4"},"readme":"asyncBuilder: to build objects… asynchronously\n==================================\n\nDealing with nested asynchronous javascript is little fun and is virtually impossible to optimize by hand with any reasonably complicated block of code. Nested sets of functions will run at the speed of their slowest calls combined, regardless of what the actual dependency tree may look like. This little tool is built to help optimize these paths and to give some insight into what's going on.\n\nSetting up and using a builder involves the following 3 steps:\n\nCreate a BuilderFactory\n-------\n\nA `BuilderFactory` is used to create a map of functions that produce output and receive a given set of inputs. The `add` function may be called to add handlers to the BuilderFactory with a name, function, and list of inputs into the function (from other handlers' output or data passed into `build()` of the `BuilderInstance`).\n\n```javascript\nvar builderFactory = new require(\"asyncBuilder\").BuilderFactory\n\nbuilderFactory\n  .add(\"firstName\", getFirstName, [\"user\"])\n  .add(\"lastName\", getLastName, [\"user\"])\n  .add(\"fullName\", getFullName, [\"firstName\", \"lastName\"])\n\nfunction getFirstName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.firstName)\n}\n\nfunction getLastName(user, next) {\n  if (!user) return next(new Error(\"User does not exist\"))\n  next(null, user.lastName)\n}\n\nfunction getFullName(firstName, lastName, next) {\n  next(null, firstName + ' ' + lastName)\n}\n```\n\nAs a note: any errors passed into the first parameter of `next` for a given handler will end the execution of the current `build()` call (with the exception of any calls in progress which will silently be ignored) and will bubble up to the callback specified to the `build` step.\n\nYou may also provide the name of one handler as the function for another if you wish to provide another accessor:\n\n```javascript\nbuilderFactory\n  .add(\"shortName\", \"firstName\")\n```\n\n`BuilderFactory` instances may be cloned as a method of extending them without overriding (or adding to) the handlers on the original factory:\n\n```javascript\nvar newBuilderFactory = builderFactory\n  .clone()\n  .add(\"fullName\", getAbbreviatedName, [\"firstName\", \"lastName\"])\n  .add(\"shortName\", \"lastName\")\n```\n\nCreate a `BuilderInstance`\n-------\n\nThe `BuilderFactory` can create `BuilderInstance` objects via the `newBuilder` method. Each `BuilderInstance` will have a code path optimized for it's specific set of required output fields. Only the code needed to fulfill a given instance's outputs will ever be ran by a given `BuilderInstance`.\n\n```javascript\nvar myBuilder = builderFactory.newBuilder([\"fullName\", \"shortName\"])\n```\n\nBuild your object\n-------\n\nYour `BuilderInstance` can now be used to asynchronously generate output via the `build` method.\n\n```javascript\nmyBuilder.build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nIf you have a need to further inspect what's happening with the asyncBuilder, a utility method `trace()` has been exposed on the `BuilderInstance` and will show timing information and execution steps for a given `build()` call:\n\n```javascript\nmyBuilder\n  .trace()\n  .build({user: {firstName: \"Jeremy\", lastName: \"Stanley\"}}, function (err, data) {\n  //data.fullName and data.shortName should both be present\n})\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@0.2.21","dist":{"shasum":"527cbacdae31d2261880d05f6da8f8c544fed746","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-0.2.21.tgz","integrity":"sha512-PfIrLaq2sseUSRmsooqR0dMQa99lfXRQHnNGBt7RHm3LAdSvi4WiQ+WSq1xC6aMzOfrVo5oXHHsYJ2GghL+wag==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQC0tbY7JbDEwKoZ5hWITO4MeD/KZnCVmWJDlN+5/OE1BQIgYEuzumLCYpOQTN5uNQvp3Polc4zVnbP/GCEriD4ZUW4="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"1.0.0":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.0.0","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"*","kew":"*","oid":"*"},"devDependencies":{"nodeunit":"*"},"scripts":{"test":"nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the first parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.0.0","dist":{"shasum":"aa11f1af466e7262f77cf9d7a5df713efb68e5fc","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.0.0.tgz","integrity":"sha512-057b8ioWW3uLOlOaW6zZm/GuYHFic0WXBp3hvZb3N9+gwqtf0d2L2sGyhlaPDVgdlaHygZOu+ntNsAi1dkMLkw==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEYCIQDSapP8iVCOu8WV0t67a4heJ0sjMmBb6+bdlz41Ktk5JQIhAMesMrdJhVDGR0j5ePzDj5d8phva+sSSVjT3x411/L85"}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"1.0.1":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.0.1","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"*","kew":"*","oid":"*"},"devDependencies":{"nodeunit":"*"},"scripts":{"test":"nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the first parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.0.1","dist":{"shasum":"7a3748f78b58d37963abe036f68a03f5538d8ff0","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.0.1.tgz","integrity":"sha512-b5fZXtu/cjG41FVGWIDfklgxi3VIxcl7SnmreY2zijE+1L9Z1BrpHvAl4qZ/JBhU7K8NXhKLGHefm1yEEv+qLA==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCIDEK7Od56xkRUgRplh+6HCSqfS/dqquZWH0e4OyHzkZJAiBQ/vmPyQzqgeulhFV9hmRkdsIvzVCU6+aRa/AOFpoKsg=="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"1.0.2":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.0.2","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"microtime":"*","kew":"*","oid":"*"},"devDependencies":{"nodeunit":"*"},"scripts":{"test":"nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the first parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.0.2","dist":{"shasum":"5253d3e005dafcad4acd518438a2c3c79ba23672","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.0.2.tgz","integrity":"sha512-ZwUDpdTJyrMLkkzWA+aa0x2N5yAZ1pLc0UQ1niTwnQfd2DAqarZDJhf+4O+kPYxs5KhKeoCZRKHaaSxH5TvQUA==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEYCIQCu9huVsXPzshhOoj0x5EK01CoEX5NJ62fEzOYnP9cYfQIhAKmZzxbenSI9xYt3KTfVSYDjVENxVT8DlSc0I8uHz7JN"}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"1.0.3":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.0.3","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","microtime":"0.3.1","kew":"0.0.4","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the first parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.0.3","dist":{"shasum":"0fa2f7e72626f408c0ea93089febb4742bd644f5","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.0.3.tgz","integrity":"sha512-ooopPjesTDS/rJfFgtWIEwR//ZNtHYj5wSXGYZr70Vw6yvEnqMxX2S4JTXQ62W1iRsljBck/OhT4Wd7JOy9CPg==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIFYLSaVVbI/cAWswVVgThHbDIeT9h/X4AJStKyWo5s5KAiEA3YhIedG9nM/Wk1p3p3P/QEwY9U19Ql/SNdzkd4mjy6M="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"1.0.4":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.0.4","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.0.4","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the first parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.0.4","dist":{"shasum":"9c6288a559ff8c465bfafe4f1707b10dcf95d8c8","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.0.4.tgz","integrity":"sha512-/aZteIZQNcp74SMRyAU00pK/FZ1YgcgZ8fum+zYBFpe689nb4BSSKcqm/OZUz6MhZo3GSd6pi48lMMNuLvgglw==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCICD+rdpaCcho44Bu3YcYvtvNGCoQ2JGgRPPoexQGtOJSAiEA4UPILYwMoyNrRWpIni167tiizXthYjzXvlGC+XsK3pg="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"1.0.5":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.0.5","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.0.4","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the first parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.0.5","dist":{"shasum":"576d7a05d3f8e3b102c813d7f80fb37cb3d26e7b","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.0.5.tgz","integrity":"sha512-TU8SfjcwRmngY+pSD6zENnwD7ZO0WN95F4DjEB7p1daSMlStnAKw4tftvy5fU0GoUuvI4FhM3uifAU9xYuXhFA==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIAdA1nT7su3GqLsaDcK84cRkhD5wTaGNfz05ntYIQWZSAiEA5AYsajor77nlrs9sclY4OQHa2cb8UZ/NGKnXoKDj0Uk="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"1.0.6":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.0.6","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.0.4","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the first parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.0.6","dist":{"shasum":"84f9bf8a9727e228d1e6e24efc4ddfafc86257c0","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.0.6.tgz","integrity":"sha512-At6tDatmkfTxghqlerqcRipputbaheoWaSNzaekwTE+jogtxRmlVbu9HdNnIpQOD7nwg2wvhfEW05H8Fz9pHeQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIBNIMpoI3w675/B3gIwfPr5oexo9ML94rORJXk/eNByMAiEA2CeFwHajjfjmGHIJtJVDQkhiDjiB2iA9hNuxk9x4sFU="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"1.0.7":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.0.7","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)","Elizabeth Ford <eford@obvious.com> (https://github.com/eford1)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.0.4","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"./node_modules/nodeunit/bin/nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the first parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.0.7","dist":{"shasum":"78cac2a72870602e8149c83dc4545e3d57970c66","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.0.7.tgz","integrity":"sha512-pyF74nKk+/dS+Db5PxjEf3Xzp1h3mqBMrFi9bJCVu3OIT9yV7F2ZCCPuWPWT8c6cT9UAX/trO7BUU7CI2vuKhQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCICdZtjreOCoQ1GMDTU85bg1lRnOEOHSv5C7+TP020FkcAiBBIfYkfbdV0YJGfUsults9iljoOkf8BpBY3PpUV7YnNg=="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"}],"directories":{}},"1.0.8":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.0.8","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)","Elizabeth Ford <eford@obvious.com> (https://github.com/eford1)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.0.4","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"./node_modules/nodeunit/bin/nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the first parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.0.8","dist":{"shasum":"d828b71343616a441091eabbac2c5a713b64cdb2","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.0.8.tgz","integrity":"sha512-G8CN9M84vvNo+WqMfsIjRDxN409AhjuzXAZudlzXFg83xHwZKOmA9gzFQwCBVbgjVXe9FQWDqKqZiZyxTRJ9Qg==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEYCIQChU/eE7K18cUC7wUgMmu7Ip1DtI/N8DHZG4xnlq8SCngIhANMilE+pLYaWJw6EvI7YP/yktpmxpbYDBKWXW7IkAVnG"}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"},{"name":"eford","email":"eford@obvious.com"}],"directories":{}},"1.0.9":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.0.9","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)","Elizabeth Ford <eford@obvious.com> (https://github.com/eford1)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.0.4","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"./node_modules/nodeunit/bin/nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the last non-callback parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.0.9","dist":{"shasum":"2d0022aba5fe4561cd2a150a5e35126a6055cb40","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.0.9.tgz","integrity":"sha512-JOPqe4KIlz35Nm31EayW5O/VFffOwzA41wNxef/X3b3LRzuTpGekWDtKnjm7nvkUWP6Ng9/scOg8CxX086PskA==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQDElXg11djZHs0dWLx+F2wEYbQrp0K21KL+/2mQyXSBvwIgcedOn5lwoDDsAEopx+Mk4KHysoVbAzHLVFrj+mIBCzk="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"},{"name":"eford","email":"eford@obvious.com"}],"directories":{}},"1.1.0":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.1.0","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)","Elizabeth Ford <eford@obvious.com> (https://github.com/eford1)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.0.4","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"./node_modules/nodeunit/bin/nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the last non-callback parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.1.0","dist":{"shasum":"be596efe9b8debb7fd17b8a89b4d88e83345d470","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.1.0.tgz","integrity":"sha512-7ScDU4UpHqXsT2VSr53Jn5+GCA22/H43xXxFg/Gcq9Q/8J0DA9k8UDhCCgj0kyXqrjPyEejSf85m+DWoSAJAhg==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCICVKqiiZ5saUvcUxipZJtxdtza3qd44xL6hAhYGIYpmJAiEAkg2hNAHYd2onVEwlf5RBzr90WCkbcAcBhtuPnwi17t0="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"},{"name":"eford","email":"eford@obvious.com"}],"directories":{}},"1.1.1":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.1.1","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)","Elizabeth Ford <eford@obvious.com> (https://github.com/eford1)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.0.4","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"./node_modules/nodeunit/bin/nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the last non-callback parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.1.1","dist":{"shasum":"970d9d3cf459d4c98cf8b65c4cbd556b80ef45b7","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.1.1.tgz","integrity":"sha512-7f+aEx0JXyGhzjfLnLaGOHoja86Irb0FNt8kszdD4Ir08UJWgl4hepkFu8NF92wYkBXW7VuTp0DRClE2vgejjA==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEYCIQCqKNrpxu8xRTJIRsm+zUnwf0DukMY/eAtBa9sTf4VbQQIhAOJxEcpxSbb6E/w4ILfoun7egpnrMT17pHHc+rZBtW2f"}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"},{"name":"eford","email":"eford@obvious.com"}],"directories":{}},"1.1.2":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.1.2","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)","Elizabeth Ford <eford@obvious.com> (https://github.com/eford1)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.0.4","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"./node_modules/nodeunit/bin/nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the last non-callback parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.1.2","dist":{"shasum":"8dac3435e90a8c46e32e7d38ac3a5498f9c8cd82","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.1.2.tgz","integrity":"sha512-BlMAluxcBr7gGTBxW2/a5Z6eqtitQpG7hjjI4w3PSjg/POMde4DmEiwp7Dk2EpZ+2mZnUI24Qm5tZ3RkHDkCWw==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEYCIQCBTAu6bENbIxy4xmRqnRm9rlgfz4ioFKDLZd0K+XZHwQIhAM8TKlyaLnUCi39z9YIHHdDDElpNM5Gxeumlcemqi9lV"}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"},{"name":"eford","email":"eford@obvious.com"}],"directories":{}},"1.1.3":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.1.3","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)","Elizabeth Ford <eford@obvious.com> (https://github.com/eford1)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.0.4","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"./node_modules/nodeunit/bin/nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the last non-callback parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.1.3","dist":{"shasum":"45715efd7c31826bc3c7d7540ec8fd6abb9aac73","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.1.3.tgz","integrity":"sha512-Yq8OAoh3hr9jntG/Eqsg0nzFVNiJ+jMMNPzQ5HuxzRPYeNZA7maHrB8+6Bo3kbFNFbTS/G8JWSG/wk5TVVEjvA==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIFwG58s3jRMgwhSyHK3OR10Qk4V1NSZNkNtMQdnLtkg4AiEAu0H7lSd5LornxeuItLtupZxAZsr0nn/W42KvBFmU6Yw="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"},{"name":"eford","email":"eford@obvious.com"}],"directories":{}},"1.1.4":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.1.4","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)","Elizabeth Ford <eford@obvious.com> (https://github.com/eford1)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.0.4","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"./node_modules/nodeunit/bin/nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the last non-callback parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.1.4","dist":{"shasum":"f4a187efea0de038e25ac2e35f3a359774e286d9","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.1.4.tgz","integrity":"sha512-VvmANTMN3YrfM4hgFCRJxJix41+NzIdLtPmxPlqde4oHw5QTHnp//LZOFJD2VUhatxAJznoHJ9V+9uPqX1hiQg==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIH5aqKnellvR64b3ypChI11PWWX63sxtHbbFjZ4jEJORAiEApMLWsQBLo7I1GpsvM/0rQp+YeXqcq1qhhZVXnUwqLFQ="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"},{"name":"eford","email":"eford@obvious.com"}],"directories":{}},"1.1.5":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.1.5","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)","Elizabeth Ford <eford@obvious.com> (https://github.com/eford1)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.0.4","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"./node_modules/nodeunit/bin/nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the last non-callback parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.1.5","dist":{"shasum":"d2a2ee2c43c14d8366ca58b9edbe79329c035800","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.1.5.tgz","integrity":"sha512-NJBW8RuByAaUSRONverXwtMApGhm0RN3JNpW9Z3pKVxP1B5OSd/KY8IFE7O+8bMrK9/Rwqgubjz7+nVhTzYaDQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQC6KNKbXAGn9Gmxcnv1Y6dLmO6uLROYuJD8H6jsjoZ5XgIgBrVz0dqugmMM5zEiLibPbjTEebXVmBCU/KJ3JcKLcE8="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"},{"name":"eford","email":"eford@obvious.com"}],"directories":{}},"1.1.6":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.1.6","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)","Elizabeth Ford <eford@obvious.com> (https://github.com/eford1)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.0.4","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"./node_modules/nodeunit/bin/nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the last non-callback parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.txt` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.1.6","dist":{"shasum":"7f76977b6d123455e2bc8cb2108131ab6c1113b4","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.1.6.tgz","integrity":"sha512-cjmLlczz37EtqOGVtI6V+vV4ty+p/7vy/FWd46T+2/nLbQlgRatntOIH5a6fO/NWYM3iHN/hF/SKUe1SSKZksQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEQCIGtUk/lSqdoJfxqdZbkWkKMk94EEPovOhg5rfOG3YEfOAiBU8H+jh351Vhb1nJvhrwPCdxzjiyb01qZxaD2Ycw9LFA=="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"},{"name":"eford","email":"eford@obvious.com"}],"directories":{}},"1.1.7":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.1.7","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)","Elizabeth Ford <eford@obvious.com> (https://github.com/eford1)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.1.0","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"./node_modules/nodeunit/bin/nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the last non-callback parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.TXT` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.1.7","dist":{"shasum":"462795bd8921e8e508b20d65ffe3876ab61547f6","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.1.7.tgz","integrity":"sha512-6yKA1EEhNVuXkx66wOEUctWrjP+Zl7BF95vyW8EMcOma9sWYHr2/e5qSjDazN7n0i4q73dZ82nkdoylJxyNQ/Q==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIQDgrzgDyYDjKXMr88y7b/Zl28V/Mq4L1VquCVttw82pLwIgSqf5fMurD4fLCUSIdKfNO4XSYHkEH4WWjt3WeNZoemg="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"},{"name":"eford","email":"eford@obvious.com"}],"directories":{}},"1.1.8":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.1.8","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)","Elizabeth Ford <eford@obvious.com> (https://github.com/eford1)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.1.0","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"./node_modules/nodeunit/bin/nodeunit test"},"readme":"asyncBuilder: asynchronous dependency injection and more!\n==================================\n\n**asyncBuilder** is dependency injection via a `Graph` of dependencies and a `Builder` which is used to define which of those dependencies you need at any given point in time. Dealing with nested functions (in the best of cases) becomes hard to optimize when you have varied dependencies and **asyncBuilder** is here to allieviate that pain while abstracting away the complexity of the underlying `Graph` and allowing for easily testable bits of code.\n\nGetting started\n-------\n\nTo get started with **asyncBuilder**, you need to create a `Graph`. A `Graph` is a registry of all of the things you want to be able to do (units of work). First, instantiate the Graph:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nNext, you need to add some nodes to the `Graph` which perform said units of work. Let's add 2 nodes to the Graph:\n\n```javascript\n// create a node which returns the current timestamp in millis\ngraph.add('timestamp-nowMillis', Date.now)\n\n// create a node which will uppercase a string\nfunction toUpper(str) {\n  return str.toUpperCase()\n}\ngraph.add('str-toUpper', toUpper, ['str'])\n```\n\nNow that you have a `Graph` of things that can be done, you need to create a `Builder` which will connect those different pieces together to produce a desired result. In this case we'll create a `Builder` which will uppercase an input string and return the current timestamp in millis:\n\n```javascript\nvar builder = graph.newAsyncBuilder()\n  .builds('str-toUpper')\n  .builds('timestamp-nowMillis')\n```\n\nFinally, you can run the `Builder` with a given set of inputs and it will optimally run through the units of work:\n\n```javascript\nbuilder.run({str: \"Hello\"}, function (err, data) {\n  // data['str-toUpper'] should be HELLO\n  // data['timestamp-nowMillis'] should be the current timestamp in millis\n})\n```\n\nAnd that's the simple flow through **asyncBuilder**, though there's a lot of additional functionality listed below.\n\nWhat is the Graph really?\n-------\n\nThe `Graph` in asyncBuilder is the place where you put all the things your application is able to do. Before you add any nodes to a `Graph`, you'll need to make sure to instantiate it first:\n\n```javascript\nvar graph = new require(\"asyncBuilder\").Graph\n```\n\nAdding nodes to the Graph\n-------\n\nIn order to make the `Graph` do some work for you, you'll have to add some nodes with `Graph#add`. The first argument is the name of the node (which will be used to reference this node later), the second is the handler function to perform the work of this node, and the third is an optional array of arguments to be passed in to this node (which may be passed separately via .args()):\n\n```javascript\n// add a function to uppercase an input string by passing the arguments in .add()\ngraph.add(\"str-toUpper\", function (str) { return str.toUpperCase() }, ['str'])\n\n// add a function to lowercase an input string by passing the arguments via chaining\ngraph.add(\"str-toLower\", function (str) { return str.toLowerCase() })\n  .args('str')\n```\n\n### About node names\n\nMany functions in **asyncBuilder** can take *either* the name of a node *or* an object (arg-to-node or node-to-arg mapping based on context) as an input. If the name of a node is passed in, the following rules are used to determine what the corresponding arg name should be (returning as soon as a rule is met):\n\n1. If the node name contains a **.** (using members, defined later), the arg name is anything to the right of the last **.** *e.g.: user.username becomes username*\n2. If the node name contains a **-**, the arg name is anything to the left of the first **-** *e.g.: user-fromSpain becomes user*\n3. If no previous match is made, the arg name is the node name *e.g.: userCredentials becomes userCredentials*\n\nThis leads to certain *suggested* naming patterns within the **asyncBuilder** world:\n\n* Nodes should be named as *TYPE_OF_RESPONSE*-*SOURCE_OF_RESPONSE* *e.g.: user-byId*\n* Nodes should be referenced as specifically as possible via members (defined later) *e.g.: user-byId.username*\n\nUsing these syntaxes provides a lot of benefits as nodes that take in args with a given name can automatically infer their inputs:\n\n```javascript\n// uppercases a name, expects a 'name' variable\ngraph.add('name-toUpper', function (name) { return name }, ['name'])\n\n// provides a name, will be provided as the 'name' arg due to the -\ngraph.add('name-fromLiteral', graph.literal('Jeremy'))\n\n// an object with a name, can be accessed as userObj.name\ngraph.add('userObj', {name: \"Jeremy\"})\n\n// DON'T WORRY ABOUT THE DETAILS WITH THE BUILDER NOW, THEY'LL BE EXPLAINED LATER\n// creates a builder which passes name-fromLiteral into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('name-fromLiteral') // this automatically figures out that it should be passed as 'name'\n\n// creates a builder which passes userObj.name into name-toUpper\ngraph.newAsyncBuilder()\n  .builds('name-toUpper')\n    .using('userObj.name') // this automatically figures out that it should be passed as 'name'\n```\n\nThese syntaxes\n\n### Returning and errors\n`Graph` nodes may choose to return or throw synchronously or they may return or throw asynchronously through a promise (we currently use the node module `kew` which is a lighter implementation of much `Q` functionality) or via a node-style callback passed in as the last argument:\n\n```javascript\n// returns synchronously\ngraph.add('result-sync', function () { return true })\n\n// returns asynchronously via promise\ngraph.add('result-promise', function () { return require('kew').resolve(true) })\n\n// returns asynchronously via callback\ngraph.add('result-callback', function (next) { next(undefined, true) })\n\n// throws synchronously\ngraph.add('throws-sync', function () { throw new Error('NOOOO') })\n\n// throws asynchronously via promise\ngraph.add('throws-promise', function () { return require('kew').reject(new Error('NOOOO')) })\n\n// throws asynchronously via callback\ngraph.add('throws-callback', function () { next(new Error('NOOOO')) })\n```\n\n### Literals\nNon-string literals may also be added as graph nodes by passing them directly to .add():\n\n```javascript\n// add a number as a literal\ngraph.add('secret-ofLifeTheUniverseAndEverything', 42)\n\n// add an object as a literal\ngraph.add('object-test', {isTest: true})\n```\n\nString literals must be passed in through a special wrapper object or through the utility method `Graph#literal`:\n\n```javascript\n// add a string as a literal with a wrapper object\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n\n// add a string as a literal with the utility method\ngraph.add('name-fromFunction', graph.literal('Jeremy'))\n```\n\nStrings require a special case due to the cloning behavior built into **asyncBuilder**. If you wish to clone a node into a node with a new name, call .add() with the old name and the new name:\n\n```javascript\n// copy one node to another\ngraph.add('name-oldNode', 'name-newNode')\n```\n\n### Modifiers\nA node may also define one or more modifiers for itself when it is added to the `Graph`. Modifiers exist as other nodes in the `Graph` and serve to transform the output of a node (asynchronously). If a modifier is added with only the name of the node, the name of the argument to use when calling the modifier is deduced from the parent's name:\n\n```javascript\n// uppercase a given string\ngraph.add('name-toUpper', function (name) { return name.toUpperCase() }, ['name'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // the parent's name is 'name-fromObject' so the input into 'name-toUpper' is inferred to be 'name'\n  .modifiers('name-toUpper', 'name-someOtherModifier')\n```\n\nYou can explicitly pass in the name of the argument for the modifier by creating an object with a key of the modifer node name and a value of the argument name (think \"into modifier *as* argument\"):\n\n```javascript\n// uppercase a given string\ngraph.add('str-toUpper', function (str) { return str.toUpperCase() }, ['str'])\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  // inferring the name wouldn't work here as 'name-fromObject' would be converted to 'name'\n  // and 'str-toUpper' is expecting 'str'\n  .modifiers({'str-toUpper': 'str'})\n```\n\nYou may also create modifiers from functions which take the node to be modified as the first argument:\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\ngraph.add('name-fromObject', {_literal: 'Jeremy'})\n  .modifiers(toUpper)\n```\n\n### Caching and de-duplication\nBy default, a `Builder` instance will merge all nodes that it finds have the exact same handler function when it runs through its compile() phase and these functions will only ever run once during a `Builder#run` call. If you wish to make sure that a node is ran every time it is referenced by another node, you can call `.disableCache()` on the node when adding the node to the `Graph`:\n\n```javascript\ngraph.add(\"timestamp-nowMillis\", Date.now)\n  .disableCache()\n```\n\n### A word about chaining\nThe methods defined above that apply to nodes when they're added to the graph (`.args()` and `.modifiers()`) must be ran before any `.builds()` calls are ran for the node in order to disambiguate what node the calls should affect.\n\nBuilding nodes\n-------\nNow that we have nodes added to a `Graph` instance, we need to actually set up relationships between and make a `Builder` which will create our output. All of the functions specified here can be applied to either a node that has been added to the `Graph` or to a node being built in a `Builder`. Basic examples will be provided for both options and the variations can be extrapolated from those.\n\n### .builds()\n`.builds()` should be called when a node needs to be ran in the current context:\n\n```javascript\nbuilder\n  .builds('name-fromLiteral') // build name-fromLiteral before returning from the builder\n\ngraph.add('name-validated', validateName)\n  .builds('name-fromLiteral') // build name-fromLiteral and pass it as an input to validateName()\n```\n\n### Member variables\nIf you only wish to retrieve a member variable of a node, you can access it using standard javascript *.* delimiters:\n\n```javascript\nbuilder\n  .builds('user.name')\n  .builds('user.emails.primary')\n```\n\n### Remapping\nNodes can be renamed / aliased at build time by passing an object to `.builds()` with the new name of the node as the key and the current node name as the value. This allows you to call a node multiple times while providing each instance with different inputs\n\n```javascript\n// DON'T WORRY, .using() WILL BE EXPLAINED SOON\nbuilder\n  .builds({'user1': 'user-byUserId'})\n    .using({userId: 1})\n  .builds({'user2': 'user-byUserId'})\n    .using({userId: 2})\n```\n\n### Silent nodes\nNodes can also be built and ran without their output being provided to the requester by prefixing the node name with **!** *e.g.: !validateEmail* (if the node is remapped, prefix the alias with **!** *e.g.: {'!validator': 'validateEmail'}*). This is particularly useful in the case of validators which may throw an `Error` if a condition isn't met. The following example uses a silent node to actually stop the work from being done:\n\n```javascript\n// create a node which will call an update e-mail function for a user but will only run if validateEmail is successful\ngraph.add('updateEmail', updateEmail, ['user', 'email'])\n  // take the email as an input\n  .args('email')\n  // call validateEmail with the email arg passed in to updateEmail\n  .builds('!validateEmail')\n    .using('args.email')\n```\n\n### .using()\nNodes defined via `.builds()` will often need to be wired up to know what context they should be called in. `.using()` provides this ability by specifying where a node should get its inputs from. Inputs may be literals (using the rules provided above), anonymous functions, other nodes, or arguments provided to the parent node (in the case of a `Graph` node).\n\n```javascript\ngraph.add('str-fromInput', {_literal: \"This is my string\"})\ngraph.add('str-toUpper', toUpper, ['str'])\n\n// call str-toUpper by piping str-fromInput into it\nbuilder\n  .builds('str-toUpper')\n    .using('str-fromInput') // automatically mapped to 'str' by the name resolution explained earlier\n\n// call str-toUpper by passing it a literal\nbuilder\n  .builds('str-toUpper')\n    .using({'str': { _literal: 'This is my string' }})\n\n// call str-toUpper by passing in an anonymous function\nbuilder\n  .builds('str-toUpper')\n    .using({'str': function () { return 'This is my string' }})\n\n// call str-toUpper from str-transformed by passing in the 'str' arg which was passed into 'str-transformed'\ngraph.add('str-transformed', transformString)\n  // take in 'str' as an arg but don't pass it through to transformString\n  .args('!str')\n  // pass the results of str-toUpper through to transformString\n  .builds('str-toUpper')\n    .using('args.str')\n```\n\n### .modifiers()\nNodes defined via `.builds()` may have modifiers added on in a manner identical to a node adding a modifier to itself (as defined above):\n\n```javascript\nfunction toUpper(str) { return str.toUpperCase() }\n\nbuilder\n  .builds('str-fromInput')\n    .modifiers('str-toUpper', {'str-toLower': 'str'}, toUpper)\n```\n\nRunning the Builder\n-------\nOnce your `Graph` has been constructed and your `Builder` has been built, you can actually do something with it! `Builder#run()` accepts an input object, which will add new nodes to the graph (data-only) at run-time, and an optional callback:\n\n```\n// node-style callback can be passed in as the second argument\nbuilder.run({name: \"Jeremy\", currentTimestamp: Date.now()}, function (err, data) {\n  // you should have either an error or data at this point\n})\n// but a kew promise is also returned!\n.then(function (data) {\n\n})\n.fail(function (err) {\n\n})\n```\n\nUtility Methods\n-------\n\n### Graph#forceClone()\nIf you wish to create a base `Graph` instance which may be extended without mutating the original, you may explicitly call `Graph#clone()` or use `Graph#forceClone()` to cause any mutating changes to create a new `Graph` instance with a copy of all existing `NodeDefinition` instances:\n\n```javascript\n// explicitly create a clone\nvar newGraph = graph.clone()\n\n// force clones for mutating changes\ngraph.forceClone()\n\n// this creates a new graph as well\nvar newGraph = graph.add('someNode', someFunction)\n```\n\n### Graph#validator()\n`Graph#validator()` may be called to create a function which takes a string inputs and validates it against a regular expression. If the regular expression fails, an Error is returned with a specified error message:\n\n```javascript\n// I know this is a horrible e-mail regex...\ngraph.add('email-validated', graph.validator(/^[^@]+@.*/, \"An invalid-email address was provided\"), ['email'])\n```\n\n### Graph#setter()\n`Graph#setter()` may be called to create a function which takes an object and a value and sets a field on the object to that value:\n\n```javascript\ngraph.add('user-setEmail', graph.setter('email'), ['user', 'email'])\n```\n\n### Graph#deleter()\n`Graph#deleter()` may be called to create a function which takes an object and deletes a specified field on the object:\n\n```javascript\ngraph.add('user-deleteEmail', graph.deleter('email'), ['user'])\n```\n\n### Graph#subgraph()\n`Graph#subgraph()` returns a function which will always return the last non-callback parameter passed into it, this is useful for created a \"subgraph\" within a `Graph` which doesn't contain any new functions but may coordinate complex operations:\n\n```javascript\ngraph.add('user-updateEmail', graph.subgraph)\n  // takes user and email as inputs but doesn't send them to graph.subgraph\n  .args('!user', '!email')\n  // set the email value on the user\n  .builds('!user-setEmail')\n    .using('args.email')\n    // validate the user has a valid e-mail address\n    .modifiers('user-validateEmail')\n  // finally, pipe the results of user-setEmail into user-save and return the result to graph.subgraph\n  .builds('user-save')\n    .using('user-setEmail')\n```\n\nUtility Nodes\n-------\n\n### _requiredFields\nNodes may use the `_requiredFields` node to reflect on what member variables are expected of them from within a given `Builder`. If a node is only ever referenced via member variables, `_requiredFields` will return an array of required member names. Otherwise, `_requiredFields` will return '*' to signify that the entire object is being asked for:\n\n```javascript\nfunction getUser(userId, _requiredFields) {\n  var user\n\n  if (_requiredFields == '*') {\n    // retrieve the entire user\n  } else {\n    // loop over the fields in _requiredFields and figure out what we actually need for this request\n  }\n\n  // construct a cookie object somehow\n  return user\n}\ngraph.add('user-byUserId', getUser, ['userId', '_requiredFields'])\n```\n\nContributing\n------------\n\nQuestions, comments, bug reports, and pull requests are all welcome.\nSubmit them at [the project on GitHub](https://github.com/Obvious/asyncBuilder/).\n\nBug reports that include steps-to-reproduce (including code) are the\nbest. Even better, make them in the form of pull requests that update\nthe test suite. Thanks!\n\n\nAuthor\n------\n\n[Jeremy Stanley](https://github.com/azulus)\nsupported by\n[The Obvious Corporation](http://obvious.com/).\n\n\nLicense\n-------\n\nCopyright 2012 [The Obvious Corporation](http://obvious.com/).\n\nLicensed under the Apache License, Version 2.0.\nSee the top-level file `LICENSE.TXT` and\n(http://www.apache.org/licenses/LICENSE-2.0).\n","_id":"asyncBuilder@1.1.8","dist":{"shasum":"13ec6461c73b49df6dd26dceae20be8319d848a0","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.1.8.tgz","integrity":"sha512-oqQARcD2+uZqJPkJLrUAsN8LQGiO9dlp+iYzwlIxtTLyDIfE50iC1BJSuJ7/b6QXSUoZuG6NyjVsfv7Db2q8TA==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEUCIAczHoAb66G11f5oUSKsvYHntoPjqA6GCK7/p0xOK8bpAiEA0PHP89mZb9kX4pgOaX26OTRjslmHi+AwUrr42Ux3cwI="}]},"maintainers":[{"name":"azulus","email":"npm@azulus.com"},{"name":"eford","email":"eford@obvious.com"}]},"1.1.9":{"name":"asyncBuilder","description":"handle async dependency loading","version":"1.1.9","homepage":"https://github.com/Obvious/asyncBuilder","authors":["Jeremy Stanley <jeremy@obvious.com> (https://github.com/azulus)","Elizabeth Ford <eford@obvious.com> (https://github.com/eford1)"],"contributors":[],"keywords":["asyncBuilder"],"main":"lib/asyncBuilder.js","repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"dependencies":{"graphviz":"0.0.8","kew":"0.1.0","oid":"0.5.0"},"devDependencies":{"nodeunit":"0.7.4"},"scripts":{"test":"./node_modules/nodeunit/bin/nodeunit test"},"readme":"**asyncBuilder** is now **Shepherd**\nPlease go to [https://github.com/Obvious/shepherd]","readmeFilename":"README.md","_id":"asyncBuilder@1.1.9","dist":{"shasum":"eab52930a5aa76bdab3d7c26d319f4e1054d4218","tarball":"https://registry.npmjs.org/asyncBuilder/-/asyncBuilder-1.1.9.tgz","integrity":"sha512-ZdUc03IqZGxiyaFIe+FnEtwihEAUaQNCNcrkQebKDAs+3Aall1iJWPOvXGa/wwI52gnvq1tsJpBOe9hpXKFGzQ==","signatures":[{"keyid":"SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA","sig":"MEYCIQCkWs3Fs6JIeuJKshHmhQE4o7X83H/GN1EhBDHvILhuEQIhAJr4sFrQy0+I+DjPqZdLsm5F9MDnEsrIN+dvvxP7fEn6"}]},"_from":".","_npmVersion":"1.2.2","_npmUser":{"name":"azulus","email":"github@azulus.com"},"maintainers":[{"name":"azulus","email":"npm@azulus.com"},{"name":"eford","email":"eford@obvious.com"}]}},"readme":"","maintainers":[{"name":"azulus","email":"npm@azulus.com"},{"name":"eford","email":"eford@obvious.com"}],"time":{"modified":"2022-06-13T03:38:38.886Z","created":"2012-03-29T23:00:41.020Z","0.1.0":"2012-03-29T23:00:42.249Z","0.1.1":"2012-03-30T20:03:19.622Z","0.1.2":"2012-03-30T21:49:52.446Z","0.1.3":"2012-04-03T06:51:52.621Z","0.1.4":"2012-04-08T06:26:16.102Z","0.1.5":"2012-04-08T19:05:13.721Z","0.1.6":"2012-04-08T19:08:29.190Z","0.1.7":"2012-04-08T19:12:11.226Z","0.1.8":"2012-04-08T19:16:52.114Z","0.1.9":"2012-04-08T23:52:20.973Z","0.1.10":"2012-04-09T21:44:11.629Z","0.1.11":"2012-05-01T01:59:30.182Z","0.1.12":"2012-05-09T16:47:43.929Z","0.1.13":"2012-05-20T01:03:37.457Z","0.1.14":"2012-05-24T21:56:49.146Z","0.1.15":"2012-06-19T18:54:15.021Z","0.1.16":"2012-06-29T16:54:42.543Z","0.1.18":"2012-07-02T21:31:35.289Z","0.1.19":"2012-07-02T21:59:18.530Z","0.1.20":"2012-07-03T00:14:54.046Z","0.1.21":"2012-07-09T23:53:33.501Z","0.1.22":"2012-07-11T18:05:19.732Z","0.1.23":"2012-07-11T21:55:23.021Z","0.1.24":"2012-07-11T22:05:56.240Z","0.1.25":"2012-07-12T19:59:22.352Z","0.1.26":"2012-07-13T01:53:21.637Z","0.1.27":"2012-07-13T02:01:08.164Z","0.2.0":"2012-07-16T02:48:31.445Z","0.2.1":"2012-07-16T16:22:33.280Z","0.2.2":"2012-07-16T16:25:17.002Z","0.2.3":"2012-07-16T16:31:21.369Z","0.2.4":"2012-07-16T16:53:00.826Z","0.2.5":"2012-07-16T23:12:59.730Z","0.2.6":"2012-07-16T23:47:46.230Z","0.2.7":"2012-07-17T17:46:20.699Z","0.2.8":"2012-07-17T23:39:37.042Z","0.2.9":"2012-07-18T01:42:52.866Z","0.2.10":"2012-07-18T01:57:37.149Z","0.2.11":"2012-07-23T22:32:53.553Z","0.2.12":"2012-07-24T18:47:15.628Z","0.2.13":"2012-07-24T21:58:42.225Z","0.2.14":"2012-07-29T03:15:14.480Z","0.2.15":"2012-07-29T03:23:27.871Z","0.2.16":"2012-08-10T21:01:24.456Z","0.2.18":"2012-08-14T17:14:45.151Z","0.2.19":"2012-08-14T18:55:56.467Z","0.2.20":"2012-08-15T17:15:46.598Z","0.2.21":"2012-08-20T17:48:15.680Z","1.0.0":"2012-09-12T03:07:46.454Z","1.0.1":"2012-09-13T18:41:40.536Z","1.0.2":"2012-09-17T04:08:43.337Z","1.0.3":"2012-09-17T18:24:03.108Z","1.0.4":"2012-10-15T20:24:52.605Z","1.0.5":"2012-10-17T21:45:21.200Z","1.0.6":"2012-10-23T19:55:05.785Z","1.0.7":"2012-10-30T23:55:14.254Z","1.0.8":"2012-10-30T23:59:35.481Z","1.0.9":"2012-11-08T22:20:36.488Z","1.1.0":"2012-11-19T07:16:00.466Z","1.1.1":"2012-11-20T01:42:42.441Z","1.1.2":"2012-11-20T18:10:42.881Z","1.1.3":"2012-11-26T04:10:20.920Z","1.1.4":"2012-12-19T21:01:39.259Z","1.1.5":"2013-01-03T00:14:16.438Z","1.1.6":"2013-01-05T00:01:23.068Z","1.1.7":"2013-01-08T18:34:54.546Z","1.1.8":"2013-01-17T22:18:42.937Z","1.1.9":"2013-02-17T02:27:30.301Z"},"repository":{"type":"git","url":"https://github.com/Obvious/asyncBuilder.git"},"users":{"fgribreau":true}}